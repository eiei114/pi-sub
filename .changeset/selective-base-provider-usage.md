---
'@eiei114/pi-sub-core': minor
'@eiei114/pi-sub-shared': minor
---

Add an optional versioned, selective read-only usage event for exact base providers. Consumers can inspect a supported provider without changing selection, querying all providers, or reusing shared provider-cache data. Bound reads by cancellation and timeout; preserve disabled settings and reject account aliases.
