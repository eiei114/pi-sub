---
"@eiei114/pi-sub-core": minor
"@eiei114/pi-sub-bar": minor
"@eiei114/pi-sub-shared": minor
---

Minor version bump for the fixed release group (`sub-core` / `sub-bar` / `sub-shared`).

No functional changes: this changeset only rolls the fixed group forward to the next
minor version so the release can ship on its own.
